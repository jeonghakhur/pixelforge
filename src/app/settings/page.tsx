export default function SettingsPage() {
  return (
    <div>
      <h1>설정</h1>
      <p style={{ color: 'var(--text-muted)', marginTop: '1rem' }}>
        Figma API 토큰, 토큰 매핑, 내보내기 경로를 설정합니다.
      </p>
    </div>
  );
}
